import { NameoneDirective } from './nameone.directive';

describe('NameoneDirective', () => {
  it('should create an instance', () => {
    const directive = new NameoneDirective();
    expect(directive).toBeTruthy();
  });
});
