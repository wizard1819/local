import { HostdDirective } from './hostd.directive';

describe('HostdDirective', () => {
  it('should create an instance', () => {
    const directive = new HostdDirective();
    expect(directive).toBeTruthy();
  });
});
